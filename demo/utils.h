#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>

/**
 * LOGICFLOW DEMO: Architectural Consistency Test
 * This function calculates the final score based on points and bonuses.
 * * NOTE: For the demo, ensure the implementation in utils.c 
 * matches this signature (3 arguments).
 */
int calculate_score(int points, int bonus, int multiplier);

#endif // UTILS_H